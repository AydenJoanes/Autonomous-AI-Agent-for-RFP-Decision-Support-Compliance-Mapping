# ==============================================================================
# RFP AGENT - ENVIRONMENT CONFIGURATION TEMPLATE
# ==============================================================================
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Update values with your actual configuration
# 3. DO NOT commit .env to version control
# ==============================================================================

# --- Environment ---
# Environment indicator (development, staging, production)
ENV=development

# --- Database ---
# Postgres connection string with pgvector support
# Format: postgresql+psycopg2://user:password@host:port/dbname
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/rfp_agent_db

# --- OpenAI Configuration ---
# API Key for OpenAI services
OPENAI_API_KEY=sk-your-api-key-here

# LLM Model to be used for reasoning and generation
LLM_MODEL=gpt-4o-mini

# Embedding Model for vector search
EMBEDDING_MODEL=text-embedding-3-small

# --- Application Settings ---
# Maximum file upload size in MB
MAX_UPLOAD_SIZE_MB=25

# Allowed file extensions for RFP documents (comma separated)
ALLOWED_EXTENSIONS=pdf,docx

# --- Logging ---
# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=DEBUG

# Base directory for log files
LOG_DIR=logs

# Log retention policy (days)
LOG_RETENTION_DAYS=30
